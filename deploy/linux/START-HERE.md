# Use the Git deployment workflow

Follow [the Git deployment guide](../GIT-DEPLOYMENT.md). Run `python3 deploy.py install` from the repository root on Linux; for later releases, use `git pull --ff-only && python3 deploy.py update`. The older archive workflow is no longer required.
