{
  "name": "shangrila-tours-marketing-tracker",
  "version": "1.0.0",
  "description": "Shared marketing plans and NPR budgets for Shangrila Tours.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check server.js && node --check db.js && node --check app.js && node --check auth.js && node --check ui.js && node --check domain.js && node --check scripts/import-json.js && node --check scripts/merge-json.js && node --check scripts/setup-env.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.3.1",
    "exceljs": "^4.4.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "lucide": "1.45.0",
    "multer": "^2.3.0",
    "nodemailer": "^10.0.9",
    "pg": "^8.23.0"
  },
  "engines": {
    "node": ">=24"
  },
  "overrides": {
    "exceljs": {
      "uuid": "^11.1.1"
    },
    "qs": "^6.16.0"
  }
}
